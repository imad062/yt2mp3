<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@android:color/black"
    >

    <EditText
        android:layout_width="match_parent"
        android:layout_alignParentTop="true"
        android:layout_height="wrap_content"
        android:hint="Paste youtube video link here"
        android:textAlignment="center"
        android:id="@+id/ytlink_activity_main"
        android:longClickable="true"
        android:textColor="@android:color/white"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Get Audio"
        android:textAllCaps="false"
        android:id="@+id/button_getAudio_activity_main"
        android:layout_below="@+id/ytlink_activity_main"
        android:layout_marginTop="32dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="#009900"
        android:onClick="clickedOnGetAudio"/>


</RelativeLayout>